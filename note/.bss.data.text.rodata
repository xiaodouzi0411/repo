.bss:
    未初始化的全局变量

.data:
    已被初始化为非零的全局变量

.rodata;
    常量数据

.text：
    代码和部分整数常量（可执行）

stack：
    1. stack存放函数的局部变量和函数参数
    2. 被调用函数的参数和返回值被存储到当前程序的栈区，之后被调用函数再为自身的自动变量和临时变量在栈区上分配空间
    3. 函数返回时，栈区的数据会被释放掉，先入后出（FILO）的顺序。

heap:
    heap用来动态分配内存，由程序自身决定开辟和释放。
